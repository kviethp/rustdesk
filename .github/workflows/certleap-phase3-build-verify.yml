name: CertLeap Phase 3 standard build verification

on:
  workflow_dispatch:

jobs:
  standard-build:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
      upload-tag: phase3-verify
